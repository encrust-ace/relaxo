name: relaxo
description: Nature player

publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 2.1.2+5

environment:
  sdk: "^3.11.1"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  date_format: ^2.0.9

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_launcher_icons: ^0.14.4

# The following section is specific to Flutter.
flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/icon/
